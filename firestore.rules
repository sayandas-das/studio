rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to the students collection for development
    match /students/{studentId} {
      allow read, write: if true;
    }
  }
}