rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows anyone to read and write to the students collection.
    // For a production app, you would want to implement more secure rules.
    match /students/{studentId} {
      allow read, write: if true;
    }
  }
}
