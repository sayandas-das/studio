rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to the students collection
    match /students/{studentId} {
      allow read, write: if true;
    }
  }
}
